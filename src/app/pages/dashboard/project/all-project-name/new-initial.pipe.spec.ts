import { NewInitialPipe } from './new-initial.pipe';

describe('NewInitialPipe', () => {
  it('create an instance', () => {
    const pipe = new NewInitialPipe();
    expect(pipe).toBeTruthy();
  });
});
